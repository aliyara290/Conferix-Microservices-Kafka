server:
  port: 8081

spring:
  profiles:
    active: dev
  application:
    name: keynote-service
  config:
    import: "optional:configserver:"
  cloud:
    config:
      uri: http://localhost:8888
